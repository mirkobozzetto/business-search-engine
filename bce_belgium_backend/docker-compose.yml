services:
  postgres:
    image: postgres:15
    container_name: bce_postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5433:5432"
    volumes:
      - bce_postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: bce_redis
    ports:
      - "6379:6379"
    volumes:
      - bce_redis_data:/data
    command: redis-server --appendonly yes

volumes:
  bce_postgres_data:
  bce_redis_data:
